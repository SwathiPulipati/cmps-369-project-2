extends layout.pug 

block content   
    h1 Delete Contact
    h2 #{c.first_name} #{c.last_name}

    p Are you sure you want to delete this contact?
    form(action='/'+c.id+'/delete', method='post')
        button(type='submit') Yes! 
        a(href='/'+c.id) Nevermind